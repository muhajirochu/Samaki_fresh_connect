<?xml version="1.0" encoding="utf-8"?>
<!-- SamakiFresh: Dark navy launch background — matches the Flutter splash screen
     so there is NO white flash when Android starts the app. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Deep navy brand colour — same as splash screen background -->
    <item android:drawable="@color/launch_background_color" />
</layer-list>
